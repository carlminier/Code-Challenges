Frequency Counters

This pattern uses objects or sets to collect values/frequencies of values. Time complexity O(n).

This can often avoid the need for nested loops or O(n^2) operations with arrays/strings
